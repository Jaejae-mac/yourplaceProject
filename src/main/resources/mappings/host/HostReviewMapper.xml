<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="HostReviewMapper">


<resultMap type="HostReviewVO" id="getAllReview">
		<result property="reservNum" column="rsv_num" />
		<result property="placeNum" column="place_num" />
		<result property="memId" column="user_id" />
		<result property="star" column="review_guest_rate" />
		<result property="regDate" column="review_guest_regd" />
		<result property="review" column="review_guest_wr" />
		
	</resultMap>

	
	<select id="getAllReview" resultMap="getAllReview">
	select * from reviewGuest inner join place on reviewGuest.place_num=#{placeNum} and place.place_num=#{placeNum} inner join member on member.user_id=#{memId} and place.user_id=#{memId}
	</select>
	
	
	<insert id="insertReview">
	insert into reviewHost(rsv_num, place_num, user_id, review_guest_rate, review_guest_wr)
	values(rsv_num=#{reservNum}, place_num=#{placeNum}, user_id=#{memId}, review_guest_rate=#{star}, review_guest_wr=#{review});
	</insert>
	
	



</mapper>